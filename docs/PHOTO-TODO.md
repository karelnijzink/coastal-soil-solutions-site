# Real-photo TODO list (for Matt)

Per the imagery authenticity rule (PLAN.md): generated images are mood/texture placeholders only. These slots should be replaced with real photos as soon as Matt supplies them. The rest of the generated set (textures, scene-setting) can stay permanently.

## Replace when real photos exist

| Slot | Current file(s) `assets/img/` | What to shoot |
|---|---|---|
| Homepage hero | `home-hero-hands-compost-*` | Matt's hands holding finished compost from the farm, outdoors, overcast light, landscape orientation |
| FPJ product card (home + FPJ page) | `fpj-jug-greenhouse-*` | Real FPJ half-gallon jug **with its actual label**, on a bench/shelf — ideally on the shelf at It's About Thyme |
| Services hero | `services-hero-soil-core-*` | Matt pulling a real soil core/sample on a client site or the farm |
| Schools hero | `schools-hero-raised-beds-*` | Real school-garden session (needs school + parent consent for anything with kids; hands-only framing avoids consent issues) |
| Schools card | `schools-card-planting-*` | Same session, close-up of planting |

## Wanted but with no generated placeholder (never generate these)
- **Portrait of Matt** for the About page — real photo only, no slot exists until one is supplied.
- It's About Thyme shelf photo with FPJ stocked (with their permission).
- Microscopy shots of soil biology (signature asset — real only).
- Before/after program photos (real only, with client permission).

## How to swap a photo
Resize/export at the same widths as the files being replaced (e.g. 1920/1280/640 for heroes), WebP if possible (JPEG fine — then ask Karel/Claude to convert), upload to `assets/img/` with the same filenames, commit. The pages update automatically.
